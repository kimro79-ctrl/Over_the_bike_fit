workflows:
  android-workflow:
    name: Android Release Build
    # 마침표(.)는 깃허브 저장소의 가장 첫 화면(루트)을 의미합니다.
    working_directory: . 
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Build APK
        script: |
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
